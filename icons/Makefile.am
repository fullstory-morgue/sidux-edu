KDE_ICON = AUTO

sharedir = $(pkgdatadir)
install-data-local:
	$(mkinstalldirs) $(DESTDIR)$(sharedir)
	$(mkinstalldirs) $(DESTDIR)$(sharedir)/common
	cp -aR $(srcdir)/icons $(DESTDIR)$(sharedir)/common/